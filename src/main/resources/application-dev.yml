spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transformer
    username: postgres     # <-- your actual PG user
    password: dulmin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
